{
  "name": "marginalia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "typecheck:room": "tsc --noEmit -p tsconfig.room-check.json",
    "check:entitlements": "! grep -rn \"plan === '\" src/ --exclude='entitlements-store.ts'",
    "test:rules": "vitest run tests/firestore-rules.test.ts",
    "i18n:sync": "npx tsx scripts/i18n-sync.ts"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/three": "^0.160.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "@sentry/browser": "^10.51.0",
    "posthog-js": "^1.372.6",
    "three": "^0.160.1",
    "zod": "^4.4.2"
  }
}
